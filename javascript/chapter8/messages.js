const messages = ["Hello", "Less is more", "Simple is better than complex"];
showMessages(messages);

/** @param {string[]} messages */
function showMessages(messages) {
	for (const v of messages) console.log(v);
}
