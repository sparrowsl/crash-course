const pizzas = ["pepper", "pizza", "phone", "cheese", "vanilla", "laptop", "watch"];

console.log("The first three items in the list are:");
console.log("\t", pizzas.slice(0, 3));

console.log("Three items from the middle of the list are:");
console.log("\t", pizzas.slice(pizzas.length / 2, pizzas.length / 2 + 3));

console.log("The last three items in the list are:");
console.log("\t", pizzas.slice(pizzas.length 3));
