const locations = ["Sychelles", "Iceland", "France", "Romania", "Silicon Valley"];
console.log(locations);

locations.sort(); // sort the list in asc order
console.log(locations);

locations.reverse(); // sort the list in desc order
console.log(locations);
